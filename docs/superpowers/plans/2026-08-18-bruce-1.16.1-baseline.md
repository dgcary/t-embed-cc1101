# Bruce 1.16.1 Baseline Import Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Establish Bruce 1.16.1 as the immutable upstream source baseline for dgcary/t-embed-cc1101 without adding custom functionality.

**Architecture:** Preserve the Bruce 1.16.1 source tree as the baseline, record the exact upstream commit, and verify the LilyGO T-Embed CC1101 PlatformIO target and hardware pin definitions before any Cary-specific changes. Future work branches from this baseline.

**Tech Stack:** ESP32-S3, Arduino/PlatformIO, Bruce firmware 1.16.1, Git/GitHub.

**Spec:** User-approved conversation requirement: use Bruce 1.16.1 stable as the development base for an external-antenna LILYGO T-Embed CC1101 Plus.

## Global Constraints

- Upstream source ref: BruceDevices/firmware `1.16.1`.
- Resolved upstream commit: `ba519c936c87b89c9c667c15b932d7faca5360d5`.
- Resolved upstream root tree: `b3e5ddd0ff0271a13d879883ebaf542ca42a8818`.
- Target repository: `dgcary/t-embed-cc1101`.
- Hardware baseline: LILYGO T-Embed CC1101 Plus, external-antenna version.
- PlatformIO environment: `lilygo-t-embed-cc1101`.
- Do not add or change product features in this phase.
- Preserve Bruce AGPL-3.0 licensing and upstream attribution.

---

### Task 1: Establish repository bootstrap and isolated baseline branch

**Files:**
- Create: `docs/superpowers/plans/2026-08-18-bruce-1.16.1-baseline.md`

**Interfaces:**
- Consumes: empty `dgcary/t-embed-cc1101` repository.
- Produces: a first commit from which an isolated baseline branch can be created.

- [ ] Create this plan as the initial repository file.
- [ ] Create branch `baseline/bruce-1.16.1` from the bootstrap commit.
- [ ] Verify the branch exists before importing source.

### Task 2: Import the exact Bruce 1.16.1 source tree

**Files:**
- Replace branch working tree with upstream tree `b3e5ddd0ff0271a13d879883ebaf542ca42a8818`.

**Interfaces:**
- Consumes: upstream commit/tree IDs above.
- Produces: source tree corresponding exactly to Bruce `1.16.1`.

- [ ] Create a commit on `baseline/bruce-1.16.1` using the resolved upstream source tree.
- [ ] Verify `platformio.ini`, `LICENSE`, and `boards/lilygo-t-embed-cc1101/` are present.
- [ ] Verify `platformio.ini` contains `lilygo-t-embed-cc1101`.
- [ ] Verify board pins include CC1101 and nRF24 definitions for the Plus hardware.

### Task 3: Baseline verification and handoff

**Files:**
- No source modifications.

**Interfaces:**
- Consumes: imported baseline branch.
- Produces: a precise Codex build/flash validation target.

- [ ] Record the resulting baseline commit SHA.
- [ ] Confirm no Cary-specific feature changes were introduced.
- [ ] Provide Codex build command: `pio run -e lilygo-t-embed-cc1101`.
- [ ] Defer hardware flash/functional validation to Codex and the physical device.
